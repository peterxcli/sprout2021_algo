#include <bits/stdc++.h>

using namespace std;

bool Back[30], front[30], vis[15][15];

int main() {
    int N, M, k, Type;
    cin >> N >> M >> k >> Type;
    for (int i = 0; i < k; i++) {
        int x, y, t;
        cin >> x >> y >> t;
        vis[y][x] = true;
        front[]
        if (t == 2) {

        }

    }
}