#include<bits/stdc++.h>
#define first x
#define second y
using namespace std;

int grid[1005][1005];
int N;

void init() {
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            grid[i][j] = 1;
        }
    }
}

int bfs(int color) {
    int ans = 0;
    
}

int max_area(int x, int y, char color) {
    
}

void solve() {
    cin >> N;
    init();
    for (int i = 0; i < 3; i++) {
        char color;
        int  x, y;
        cin >> color >> x >> y;

    }
}

int main() {
    int T;
    cin >> T;
    while(T--) {
        solve();
    }
}